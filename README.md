# Laravel Frontend for the Laravel API using Vite/React

## Getting Started

First, run the development server:

```bash
npm i (or npm i --force) depending on your node versioning.

```

Run the development server:

```bash
npm run dev

```
